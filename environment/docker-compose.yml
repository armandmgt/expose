version: '3'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - '5433:5432'
    volumes:
      - 'db:/var/lib/postgresql/data'
  redis:
    image: redis:7
    ports:
      - '6389:6379'

volumes:
  db:
