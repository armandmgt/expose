<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= content_for?(:title) ? "#{content_for(:title)} - " : "" %>Expose</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<%= render 'shared/header' %>

<%= yield %>

<%
  [notice && { icon: :'bell-fill', icon_name: 'Bell icon', message: notice },
    alert && { icon: :'alert-fill', icon_name: 'Alert icon', message: alert }].compact.each do |flash|
%>
  <div data-controller="flash" class="fixed right-5 bottom-5 flex items-center w-full max-w-xs p-4 text-gray-500 bg-white rounded-lg shadow" role="alert">
    <div class="inline-flex items-center justify-center flex-shrink-0 w-8 h-8 text-blue-500 bg-blue-100 rounded-lg">
      <%= octicon flash[:icon] %>
      <span class="sr-only"><%= flash[:icon_name] %></span>
    </div>
    <div class="ml-3 text-sm font-normal"><%= flash[:message] %></div>
    <button type="button" data-action="flash#dismiss" class="ml-auto -mx-1.5 -my-1.5 bg-white text-gray-400 hover:text-gray-900 rounded-lg focus:ring-2 focus:ring-gray-300 p-1.5 hover:bg-gray-100 inline-flex h-8 w-8" data-dismiss-target="#toast-default" aria-label="Close">
      <span class="sr-only">Close</span>
      <%= octicon :'x-circle' %>
    </button>
  </div>
<% end %>

</body>
</html>
